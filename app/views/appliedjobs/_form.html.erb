<%= form_with(model: appliedjob) do |form| %>
  <% if appliedjob.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(appliedjob.errors.count, "error") %> prohibited this appliedjob from being saved:</h2>

      <ul>
        <% appliedjob.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <center><div class="card text-center gradient-custom-2" style="width: 40rem;color:white">
  <div class="card-text field form-outline px-3 py-4">
  <b> <%= form.label :resume %><b>
  <%= form.file_field :resume %>
  </div>
  <br>
    
  <div>
    <%= form.label :status, style: "display: none" %>
  </div>

  <div>
  <%= form.hidden_field :job_id, value: params[:id]  %>
  </div>

  <div>
  <%= form.hidden_field :user_id, value: current_user.id %>
  </div>

  <div  class="card-text field form-outline mb-4 px-3 py-0 ">
  <br>
    <%= form.submit "Submit",class:"btn btn-group-small my-small-btn btn btn-outline-warning" %>
  </div>
  </div>
  </div>
<% end %>
